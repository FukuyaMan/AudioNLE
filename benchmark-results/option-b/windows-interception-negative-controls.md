# Windows Interception Negative Controls Status

Not run. Required fixture is nested thread-local callback marker around fixed arithmetic/preallocated copy/stack work, repeated with a separate worker doing forbidden operations. It must demonstrate clean counters, thread isolation, marker depth correctness and recursion-safe hook behavior.
