# Production SRC Skeleton Regression

`option_b_production_src_component_skeleton` passes alongside `option_b_libsamplerate_timing_contract` and `option_b_mixed_rate_src_integer_zoh`. The 44.1 <-> 48 rows retain their existing validated-v1 evidence and run through configure/plan/prepare/process/capacity/invalidate/reconstruct seams. Prepared generation N is rejected after invalidation to N+1; reconstruction derives equal physical planning with fresh adapter state. Process output is fixed-staged at <=512 project frames and planned native input <=2048 frames.

This is a compact structural regression, not a replacement for the completed phase/edit/quality gate or a deep backend realtime audit.
