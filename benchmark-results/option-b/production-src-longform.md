# Production SRC Long-Form Evidence

Validated 44.1->48 primary and 48->44.1 sanity sparse fixtures passed 1/3/6/12-hour integer planning. For each `T0`, `logical=floor(T0*Q/P)`, phase-aligned `physicalStart`, 1024-frame v1 preroll and integer discard are derived directly; no float drift or duration render occurs. Markers are deterministic from Source index at start, minute-range and hour-range positions. Retained duration-scaled decoded PCM = 0.
