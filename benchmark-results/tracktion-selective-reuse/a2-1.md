# Tracktion Selective Reuse — A2-1 Custom-source Zero-error Baseline

Execution date: 2026-09-06 (Asia/Tokyo)

Scope: A2-1 and its required reconstruction only. No A2-2/A2-3, PDC, VST3, Tail, Phase F, source resampling, GUI, persistence, or production code was executed.

## Fixture and runtime path

| Item | Value |
| --- | --- |
| Project Timeline rate | 48000 Hz |
| Source | framework-free identity, explicit native rate 48000 Hz, generated mono data `[1.0]` |
| Domain event | `int64_t` Timeline sample, varied by case |
| Default render range | `[0,4096)` Timeline samples |
| Runtime source | custom `DomainControlledSourceNode : tracktion::graph::Node` |
| Graph/player | public `tracktion::graph::SimpleNodePlayer`, 128-frame blocks |
| Output | caller-owned in-memory float buffer |
| Device / GUI / message loop | none opened or required |
| Forbidden path check | no `Edit`, `WaveAudioClip`, `WaveNodeRealTime`, source reader, resampler, clip timing, magic offset, Domain shift, or output shift |
| Pins | Tracktion `b88a6ee51913668cb53e911e030ab736b13342cf`; JUCE `37c894f83d379179b2070d437ccd0f1cd9af9576` |

For every `ProcessContext`, the node records its exact public `referenceSampleRange` and writes `1.0` only at `frame = DomainEvent - requestedRange.start`. All other frames are `0.0`. The receiver reports absolute Timeline position; `alignment error = first non-zero absolute sample - Domain event`.

## Measurements (A2-T002, A2-T003)

| Event | Render range | Requested Timeline ranges | First / last non-zero absolute sample | Alignment error | Elapsed ms |
| ---: | --- | --- | ---: | ---: | ---: |
| 1 | `[0,4096)` | contiguous 128-sample blocks `[0,128)` … `[3968,4096)` | 1 / 1 | 0 | 0.0195 |
| 100 | `[0,4096)` | contiguous 128-sample blocks `[0,128)` … `[3968,4096)` | 100 / 100 | 0 | 0.0109 |
| 1024 | `[0,4096)` | contiguous 128-sample blocks `[0,128)` … `[3968,4096)` | 1024 / 1024 | 0 | 0.0108 |
| 2048 | `[0,4096)` | contiguous 128-sample blocks `[0,128)` … `[3968,4096)` | 2048 / 2048 | 0 | 0.0105 |
| 3000 | `[0,4096)` | contiguous 128-sample blocks `[0,128)` … `[3968,4096)` | 3000 / 3000 | 0 | 0.0163 |
| 1024 | `[512,4608)` | contiguous 128-sample blocks `[512,640)` … `[4480,4608)` | 1024 / 1024 | 0 | 0.0146 |
| 1024 | `[1000,5096)` | contiguous 128-sample blocks `[1000,1128)` … `[4968,5096)` | 1024 / 1024 | 0 | 0.0147 |

Maximum alignment error: **0 Timeline samples**.

The render-start matrix leaves the absolute event at 1024: it is receiver-relative sample 512 for `[512,4608)` and receiver-relative sample 24 for `[1000,5096)`. No render-boundary correction is applied.

## Reconstruction (A2-T004)

Domain event 1024 / range `[0,4096)` was run once, its complete 4096-sample output and recorded request ranges were retained as observation only, then the player/node graph was destroyed. The same unchanged framework-free Domain fixture constructed a new player/node graph and produced identical output, identical request ranges `[0,128)` … `[3968,4096)`, first/last sample 1024, and error 0. Reconstruction elapsed 0.0146 ms.

## Commands and verification

1. `./build.ps1` configured successfully and built all targets. Its full CTest run reports A2-1 test 7 as passed in 0.01 s. The full suite exits non-clean solely because pre-existing `tracktion_feasibility_phase_e_pdc` remains the documented high-level `+2` sample failure (6/7 tests pass); this is outside A2-1 and was not altered.
2. Target-isolated build and test after the A2 source include correction:
   `cmake --build build --target tracktion_selective_reuse_a2_1` and `ctest --test-dir build -R ^tracktion_selective_reuse_a2_1_custom_source$ --output-on-failure` → 1/1 pass, 0.46 s.
3. Direct executable output recorded every matrix case above and ended with `A2-1 PASS: custom low-level source node, headless SimpleNodePlayer, zero Timeline-sample error`.

## Result

**Pass.** All required event positions and render starts observe `alignment error = 0 Timeline samples`; the requested Timeline ranges are observed directly by the custom node; and the runtime is device-free and reconstructible from unchanged Domain state. No A2-1 hard stop occurred.
